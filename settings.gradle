switch (System.properties.scalaVersion) {
        case '2.11':
                include 'delta-cassandra',
                        'delta-jdbc',
                        'delta-mongodb',
                        'delta-hazelcast',
                        'delta-redis'
                        // No delta-java
                break
        default:
                include 'delta-cassandra',
                        'delta-jdbc',
                        'delta-mongodb',
                        'delta-hazelcast',
                        'delta-redis',
                        'delta-java'
}
